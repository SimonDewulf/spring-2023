package be.ordina.scopes;

public class ScopesApplication {

  /*
        todo: create a service which injects a prototype scoped bean that initiates itself with an int initialized by Math.ranodm();
                create a test that starts the spring contexts and calls this service returning the number. debug it
   */
}
